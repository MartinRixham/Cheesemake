#! /bin/bash

is_installed() {
	brew ls --versions $1 > /dev/null;
}

if is_installed jq; then
	echo installed
else
	echo not installed
fi

if cat recipe.json | jq . > /dev/null; then
	echo valid
else
	echo not vaild
fi
